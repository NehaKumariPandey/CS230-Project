package main.java;

class NeighbourT{
    int target; /* the index of cluster node representing neighbour */
    float distance; /* distance between the nodes */
    NeighbourT next; /* linked list entries */
    NeighbourT prev; /* linked list entries */
}